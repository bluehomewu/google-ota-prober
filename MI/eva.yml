# Configure this correctly. Otherwise, the prober will not succeed in getting OTA URLs!
# All Android fingerprints follow the following format:
#   <oem>/<product>/<device>:<android_version>/<build_tag>/<incremental>:user/release-keys
#   MI/eva/eva:9/PQ3B.190705.003/4185:user/release-keys

build_tag: "PQ3B.190705.003"       # Example: RKQ1.211119.001
incremental: "4185"     # Example: 2206131341
android_version: "9" # Only put in the major version number.
oem: "MI"             # The company that made your device (e.g. Samsung)
device: "eva"          # The device code. May be same as model code.
model: "MiProjM05"           # The model code. (e.g. SM-T380)
product: "eva"         # The product code. May be same as device code or model code.
